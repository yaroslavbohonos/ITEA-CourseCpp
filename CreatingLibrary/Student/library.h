#pragma once

class Student
{
  int age, number;
public:
  Student();
  void set(int new_age, int new_number);
  int GetAge() { return age; }
  int GetA() { return age; }
  void Print();
  ~Student();
};
